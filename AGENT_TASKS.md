# 🏗️ LAWYARD v2 — Agent Task Board
> **Generated by Orion** | Auto-decomposed for parallel agent execution
> All tasks are independent unless explicitly marked with dependencies.

---

## Tier 1: Quick Wins (No Blockers)

### TASK-01: Fix Chambers Featured Bug
**File**: `apps/directory/lib/api.ts`
**Problem**: `getChambers()` hardcodes `featured: false` in the mapping, then filters by featured — so the "Featured Chambers" section on homepage is always empty.
**Action**:
- Change `featured: false` → `featured: (c.is_featured as boolean) || false` in the chamber mapping
- Also add `is_featured` column check — if it doesn't exist in the DB, add a migration or default to `true` for chambers with a non-null `focus`
- Verify the homepage `/` renders chambers after fix
**Acceptance**: Homepage "Featured Chamber Listings" section shows chambers from the database.

---

### TASK-02: Wire Content Studio to Server Actions
**Files**:
- `apps/directory/app/dashboard/publish/page.tsx`
- `apps/directory/app/actions/content.ts`
**Problem**: Publish form exists but doesn't call the server actions.
**Action**:
- Bind the article form to `publishArticle` action (FormData fields must match: `title`, `slug`, `content`)
- Bind the podcast form to `publishPodcast` action (FormData fields: `title`, `media_url`, `media_type`, `description`)
- Auto-generate slug from title on the client side before submission
- Add loading states and error handling to both forms
- Test that published content appears on homepage and lawyer profile
**Acceptance**: A logged-in user can publish an article or podcast from `/dashboard/publish` and it appears on the site.

---

### TASK-03: Fix `verified: true` Hardcode
**File**: `apps/directory/lib/api.ts`
**Problem**: Both `getLawyers()` and `getLawyerById()` hardcode `verified: true` regardless of the actual `verification_status` from DB.
**Action**:
- Change `verified: true` → `verified: l.verification_status === 'verified'` in both functions
- Update the `Lawyer` interface if needed
- Verify lawyer cards and profile pages reflect actual verification status visually (badge/styling)
**Acceptance**: Lawyers with `verification_status: 'pending'` do NOT show a verified badge. Only `'verified'` ones do.

---

### TASK-04: Update Control Plane Metadata
**File**: `apps/control-plane/app/layout.tsx`
**Problem**: Still says "Create Next App" from Next.js boilerplate.
**Action**:
- Change title to `"Lawyard Control Plane | Admin Portal"`
- Change description to `"Administrative dashboard for Lawyard v2 — lawyer verification, content management, and system monitoring."`
- Add proper `metadataBase`, OpenGraph, and favicon references matching the directory app's pattern
**Acceptance**: Browser tab shows "Lawyard Control Plane" and OG tags are correct.

---

### TASK-05: Add Error Boundaries for Supabase Failures
**Files**: Create new files in `apps/directory/app/`
**Problem**: If Supabase is paused or unreachable, pages crash with raw `ENOTFOUND` errors.
**Action**:
- Create `error.tsx` at the app root level that catches runtime errors and shows a graceful "Service temporarily unavailable" message with retry button
- Create `not-found.tsx` for 404 handling
- Wrap the homepage data fetching in try/catch so `getLawyers()` / `getChambers()` failures show empty states instead of crashing
- Add the same pattern to `search/page.tsx` and `lawyer/[id]/page.tsx`
**Acceptance**: Killing Supabase connection shows a friendly error page, not a raw stack trace.

---

## Tier 2: Feature Builds (Self-Contained)

### TASK-06: Add Admin Auth Guard to Control Plane
**Files**:
- Create `apps/control-plane/middleware.ts`
- Create `apps/control-plane/lib/supabase/server.ts` (copy pattern from directory app)
- Create `apps/control-plane/app/login/page.tsx`
**Problem**: Control Plane has zero auth — anyone on port 3000 sees admin data.
**Action**:
- Create Supabase server client in control-plane (copy pattern from `apps/directory/lib/supabase/server.ts`)
- Create Next.js middleware that checks `auth.getUser()` and redirects to `/login` if not authenticated or not admin role
- Build a minimal login page (Supabase magic link, reuse the directory auth pattern)
- Add an admin check: `profiles.role === 'admin'` — non-admins get redirected out
**Acceptance**: Unauthenticated users are redirected to login. Non-admin users cannot access the dashboard.
**⚠️ TASK-07, TASK-08, TASK-09 depend on this being completed first.**

---

### TASK-07: Build Control Plane — Lawyers Directory Page
**Files**: Create `apps/control-plane/app/lawyers/page.tsx`
**Problem**: Sidebar link "Lawyers Directory" goes to `href="#"`.
**Action**:
- Build a full CRUD page: table of all lawyers with columns (Name, Role, Location, Verification Status, Rating, Actions)
- Add filters: by verification status (pending/verified/rejected), by specialty, search by name
- Add actions: Verify, Reject, Edit (even if edit is just a placeholder modal for now)
- Reuse the existing `getAdminStats`/`getPendingLawyers` pattern from `lib/api.ts` but expand to fetch ALL lawyers with pagination
- Update sidebar `href="#"` → `href="/lawyers"` in the root layout
**Acceptance**: Admin can view, search, filter, and verify/reject lawyers from a dedicated page.
**⚠️ Depends on: TASK-06**

---

### TASK-08: Build Control Plane — Subscribers Page
**Files**: Create `apps/control-plane/app/subscribers/page.tsx`
**Problem**: Sidebar link "Subscribers" goes to `href="#"`.
**Action**:
- Build a page listing all `newsletter_subscribers` with columns (Email, Subscribed Date, Active Status)
- Add search/filter by email
- Add bulk actions: Export CSV, Toggle active/inactive
- Show subscriber growth chart (even a simple count-by-week is fine)
- Update sidebar `href="#"` → `href="/subscribers"`
**Acceptance**: Admin can view, search, and export newsletter subscribers.
**⚠️ Depends on: TASK-06**

---

### TASK-09: Build Control Plane — Content Manager Page
**Files**: Create `apps/control-plane/app/content/page.tsx`
**Problem**: Sidebar link "Content Manager" goes to `href="#"`.
**Action**:
- Build a page with tabs: Articles | Podcasts
- Articles tab: table with Title, Author, Status (draft/published/archived), Created Date, Actions (toggle status, delete)
- Podcasts tab: table with Title, Media Type, Author, Status, Actions
- Add a "Create New" button that links to a simple creation form (or modal)
- Update sidebar `href="#"` → `href="/content"`
**Acceptance**: Admin can view and manage all articles and podcasts from the Control Plane.
**⚠️ Depends on: TASK-06**
